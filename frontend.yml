- name: Front setup
  hosts: all
  become: yes
  tasks:
    - name: Install nginx
      ansible.builtin.dnf:  # Use 'yum' for older RHEL-based systems
        name: nginx
        state: latest

    - name: start nginx
      ansible.builtin.systemd_service:
        state: restarted
        name: nginx


